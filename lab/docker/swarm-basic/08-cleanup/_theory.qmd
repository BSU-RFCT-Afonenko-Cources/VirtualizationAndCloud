Очистка оркестратора должна удалять объекты конкретной лаборатории, не разрушая общую среду без необходимости. Service удаляется вместе с его tasks и endpoint. Выход из Swarm изменяет состояние всего node, поэтому в последовательном курсе manager можно оставить активным для следующей работы.
