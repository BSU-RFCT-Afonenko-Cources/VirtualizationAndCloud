:::{#exr-swarm-basic-service}
### Создание service

Создайте replicated service с именем `lab-api` из подготовленного image `swarm-api:lab`. На этом этапе сервис должен иметь одну replica без опубликованного порта. Дождитесь, пока его единственная задача перейдёт в состояние Running.
:::
