## 3. Сохранение данных

Удаление контейнера не удаляет подключённый named volume, если это не было запрошено отдельно. Поэтому stateful-компонент можно пересоздать, сохранив dataset. Проверка persistence должна читать данные через новый контейнер, а не только подтверждать существование volume.
