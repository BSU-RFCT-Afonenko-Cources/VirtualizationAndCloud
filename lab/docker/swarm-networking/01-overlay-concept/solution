#!/usr/bin/env bash
set -euo pipefail
source /opt/swarm-networking-lab/lib.sh

network_exists lab-front || docker network create --driver overlay --label "$LAB_LABEL" lab-front >/dev/null
network_exists lab-back || docker network create --driver overlay --label "$LAB_LABEL" lab-back >/dev/null
