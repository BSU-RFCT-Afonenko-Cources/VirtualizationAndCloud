# Swarm overlay networks и сервисное взаимодействие

В лабораторной работе строится трёхзвенная система `edge` → `api` → `db` в Docker Swarm. Наружу публикуется только edge-сервис, а внутреннее взаимодействие выполняется через overlay-сети и встроенное обнаружение сервисов.
