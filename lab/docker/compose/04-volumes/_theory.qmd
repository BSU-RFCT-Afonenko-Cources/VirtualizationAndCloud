Named volume имеет собственный жизненный цикл и не зависит от writable layer контейнера. Для базы это позволяет пересоздавать контейнер, сохраняя состояние. Compose маркирует volume именем проекта и логическим именем ресурса.
