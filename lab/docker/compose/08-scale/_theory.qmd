Stateless API не хранит уникальное локальное состояние и может иметь несколько взаимозаменяемых экземпляров. Общие данные остаются в базе. Compose создаёт несколько контейнеров одного сервиса, а встроенный DNS возвращает их адреса; proxy продолжает использовать service name.
