Раздел `services` описывает желаемые контейнеры приложения: образ или сборку, параметры запуска и базовые связи с файлами. Compose преобразует декларацию в контейнеры с project- и service-labels. В этой работе `web` принимает HTTP-запросы, `api` выполняет прикладную логику, а `db` хранит счётчики.
