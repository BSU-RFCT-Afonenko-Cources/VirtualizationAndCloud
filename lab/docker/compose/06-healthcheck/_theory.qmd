Состояние running означает лишь, что главный процесс не завершился. Healthcheck проверяет прикладную готовность: база отвечает на протокол, API обслуживает health endpoint, proxy принимает HTTP. Docker хранит результат отдельно от состояния процесса.
