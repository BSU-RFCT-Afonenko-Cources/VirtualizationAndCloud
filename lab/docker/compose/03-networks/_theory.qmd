Сети Compose задают границы связности. Публичный proxy и API разделяют сеть `frontend`, а API и база — сеть `backend`. Поэтому proxy не получает прямой маршрут к базе. Флаг internal у backend дополнительно подчёркивает назначение внутреннего сегмента.
