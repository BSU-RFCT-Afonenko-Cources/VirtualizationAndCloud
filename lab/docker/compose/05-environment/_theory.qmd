Переменные окружения подходят для несекретных параметров развертывания: DNS-имени сервиса, порта или режима работы. Compose DNS позволяет обращаться к базе по имени сервиса, а не по изменчивому IP-адресу контейнера. Пароли и ключи требуют отдельного механизма секретов.
