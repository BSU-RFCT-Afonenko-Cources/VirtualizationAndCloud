:::{#exr-swarm-scaling}
## Масштабирование API

Увеличьте желаемое число реплик сервиса `orders-api` до пяти. Дождитесь сходимости состояния и убедитесь, что published endpoint продолжает отвечать версией `v1` во время и после изменения.
:::
