Подход через XML подходит для повторяемой инфраструктуры: конфигурация домена хранится как файл и применяется через `virsh define`.

:::{#exm-lab-libvirt-install-define-xml-cmd}
#### Применение XML

```bash
virsh define /home/ubuntu/define-xml/alpine-xml.xml
```
:::
