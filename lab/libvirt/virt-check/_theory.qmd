Проверка окружения нужна, чтобы убедиться: аппаратная виртуализация активна, доступ к `/dev/kvm` есть, а `virsh` подключается к системному демону libvirt.

:::{#exm-lab-libvirt-virt-check-cmd}
#### URI libvirt

```bash
virsh uri
virt-host-validate -q qemu
```
:::
