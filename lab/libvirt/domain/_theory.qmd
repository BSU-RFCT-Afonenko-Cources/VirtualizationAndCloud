`virsh define` регистрирует постоянную конфигурацию домена в libvirt. Это декларативный этап: ВМ ещё не обязана быть запущенной.

:::{#exm-lab-libvirt-domain-cmd}
#### Проверка XML

```bash
virsh dumpxml student-vm
```
:::
