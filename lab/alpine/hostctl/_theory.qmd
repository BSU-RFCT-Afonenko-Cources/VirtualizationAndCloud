; при ручной настройке обычно устанавливают `tzdata` и используют файлы из `/usr/share/zoneinfo`. Для синхронизации времени Alpine предлагает NTP-демоны, например `chronyd`, которые запускаются и включаются через OpenRC. ([docs.alpinelinux.org][2])
